import { GeekMarkerPipePipe } from './geek-marker-pipe.pipe';

describe('GeekMarkerPipePipe', () => {
  it('create an instance', () => {
    const pipe = new GeekMarkerPipePipe();
    expect(pipe).toBeTruthy();
  });
});
