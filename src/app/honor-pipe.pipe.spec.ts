import { HonorPipePipe } from './honor-pipe.pipe';

describe('HonorPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HonorPipePipe();
    expect(pipe).toBeTruthy();
  });
});
